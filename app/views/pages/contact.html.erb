<div> Phone: (801) 558 4126 </div>
<div> Mailing: (Should we get a PO Box?)</div>
<div>
  <div>Email: (Please allow up to 72hrs for a reply)</div>
<%= form_for @message, url: create_message_url do |f| %>
<%= notice %>
<%= @message.errors.full_messages.join(', ') %>
<div>
  <div>
    <p><%= f.text_field :name, placeholder: 'name' %></p>
    <p><%= f.text_area :project_description, placeholder: 'body' %></p>
    <p><%= f.email_field :email, placeholder: 'email' %></p>
  </div>
</div>
<%= f.submit 'Send' %>
<% end %>

</div>

<div><%= link_to "For Business inquiries click here", new_message_path %></div> 
